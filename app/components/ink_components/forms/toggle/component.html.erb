<%= tag.label class: "inline-flex items-center me-5 cursor-pointer" do %>
  <%= tag.input **attributes %>
  <%= tag.div **div_attributes %>
  <%= tag.span text, **span_attributes if text.present? %>
<% end %>