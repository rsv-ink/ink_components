#!/usr/bin/env bash

set -e

echo "=== Install gem's ruby dependencies ==="
bundle install

echo "=== Install gem javascript dependencies ==="
yarn

echo "=== Install dummy javascript dependencies ==="
yarn --cwd spec/dummy

echo "=== Remove old assets ==="
bin/rails app:assets:clobber

echo "=== Start the services ==="
bin/dev
